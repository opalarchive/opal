import { FC } from "react";
import { ProjectViewProps } from "../../../../../utils/getProjectViewProps";

const Compile: FC<ProjectViewProps> = ({
  user,
  fbUser,
  project,
  projectEdit,
}) => {
  return <div>Compile</div>;
};

export default Compile;
