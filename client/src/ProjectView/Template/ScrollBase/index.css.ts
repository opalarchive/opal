import { createStyles } from "@material-ui/core";

const styles = createStyles({
  container: {
    width: "100%",
    height: "100%",
    position: "relative",
  },
  centered: {
    margin: "0 auto",
    position: "relative",
  },
});

export default styles;
